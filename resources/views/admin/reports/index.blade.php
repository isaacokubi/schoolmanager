@extends('layouts.admin')
@section('title','Reports | School Manager')
@section('page_title','Reports')
@section('admin_content')
<div class="reports-shell">
    <div class="admin-page-head report-hero">
        <div>
            <span class="report-kicker">Executive intelligence centre</span>
            <h1>{{ $report === 'overview' ? 'Executive School Reports' : 'School Reports' }}</h1>
            <p>{{ $report === 'overview' ? 'A concise leadership view of learner population, finance, attendance, academic achievement and admissions.' : 'Operational reporting for CBC academics, attendance, fees and admissions.' }} Generated {{ $generatedAt->format('d M Y H:i') }}.</p>
        </div>
        <div class="admin-actions"><a class="btn secondary" href="{{ route('admin.dashboard') }}">← Dashboard</a><button class="btn" type="button" onclick="window.print()">Print</button></div>
    </div>

    <nav class="report-tabs" aria-label="School reports">
        @foreach(['overview'=>'Executive Overview','students'=>'Students','fees'=>'Fees & Payments','attendance'=>'Attendance','results'=>'CBC Results','admissions'=>'Admissions'] as $key=>$label)
            <a class="{{ $report === $key ? 'active' : '' }}" href="{{ route('admin.reports',['report'=>$key]) }}">{{ $label }}</a>
        @endforeach
    </nav>

    <div class="stat-grid report-stats">
        @foreach([
            ['Students',$studentCount,'Current learner register','👥'],
            ['Parents',$parentCount,'Registered guardians','◉'],
            ['Teachers',$teacherCount,'Active teaching staff','◆'],
            ['Classes',$classCount,'Active classes & streams','▦']
        ] as $card)
            <div class="stat report-stat"><span class="stat-icon">{{ $card[3] }}</span><span class="muted">{{ $card[0] }}</span><strong>{{ number_format($card[1]) }}</strong><small>{{ $card[2] }}</small></div>
        @endforeach
    </div>

    @if($report === 'overview')
        <section class="executive-grid">
            <article class="card executive-card finance-card">
                <div class="section-head"><div><span class="page-kicker">Financial position</span><h2>Fee collection</h2><p>Recorded successful and completed payments against current learner balances.</p></div><span class="insight-icon">KES</span></div>
                <div class="finance-values"><div><span>Collected</span><strong>KES {{ number_format($paymentTotal,2) }}</strong></div><div><span>Outstanding</span><strong>KES {{ number_format($outstanding,2) }}</strong></div></div>
                <a class="executive-link" href="{{ route('admin.reports',['report'=>'fees']) }}">Open finance report →</a>
            </article>
            <article class="card executive-card academic-card">
                <div class="section-head"><div><span class="page-kicker">Academic health</span><h2>CBC achievement</h2><p>{{ number_format($resultCount) }} recorded results with a {{ number_format($averageMarks,1) }}% average score.</p></div><span class="insight-icon">CBC</span></div>
                <div class="mini-metrics"><div><strong>{{ number_format($averageMarks,1) }}%</strong><span>Average score</span></div><div><strong>{{ number_format($missedAssessmentCount) }}</strong><span>Missed assessments</span></div></div>
                <a class="executive-link" href="{{ route('admin.reports',['report'=>'results']) }}">Open CBC report →</a>
            </article>
            <article class="card executive-card attendance-card">
                <div class="section-head"><div><span class="page-kicker">Learner participation</span><h2>Attendance snapshot</h2><p>Current attendance records grouped by status.</p></div><span class="insight-icon">✓</span></div>
                <div class="status-grid">
                    @foreach(['present'=>'Present','absent'=>'Absent','late'=>'Late','excused'=>'Excused'] as $key=>$label)<div><span>{{ $label }}</span><strong>{{ number_format($attendanceSummary[$key] ?? 0) }}</strong></div>@endforeach
                </div>
                <a class="executive-link" href="{{ route('admin.reports',['report'=>'attendance']) }}">Open attendance report →</a>
            </article>
            <article class="card executive-card admissions-card">
                <div class="section-head"><div><span class="page-kicker">Enrolment pipeline</span><h2>Admissions snapshot</h2><p>Application activity by current workflow status.</p></div><span class="insight-icon">+</span></div>
                <div class="status-grid admissions-status">
                    @foreach(['approved'=>'Approved','pending'=>'Pending','rejected'=>'Rejected'] as $key=>$label)<div><span>{{ $label }}</span><strong>{{ number_format($applicationSummary[$key] ?? 0) }}</strong></div>@endforeach
                </div>
                <a class="executive-link" href="{{ route('admin.reports',['report'=>'admissions']) }}">Open admissions report →</a>
            </article>
        </section>

        <section class="card report-section executive-achievement">
            <div class="section-head"><div><span class="page-kicker">Competency-Based Assessment</span><h2>Achievement distribution</h2><p>School-wide CBC achievement levels across recorded numeric results.</p></div><a class="executive-link" href="{{ route('admin.reports',['report'=>'results']) }}">View all results →</a></div>
            <div class="cbc-summary">
                @foreach(['EE1'=>'Exceeding 1','EE2'=>'Exceeding 2','ME1'=>'Meeting 1','ME2'=>'Meeting 2','AE1'=>'Approaching 1','AE2'=>'Approaching 2','BE1'=>'Below 1','BE2'=>'Below 2'] as $code=>$label)
                    <div><b>{{ $code }}</b><strong>{{ $cbcLevelSummary[$code] ?? 0 }}</strong><small>{{ $label }}</small></div>
                @endforeach
            </div>
        </section>

        <div class="executive-lower">
            <section class="card report-section compact-report"><div class="section-head"><div><span class="page-kicker">Latest activity</span><h2>Recent payments</h2></div><a class="executive-link" href="{{ route('admin.reports',['report'=>'fees']) }}">View all →</a></div><div class="table-wrap"><table class="table"><thead><tr><th>Date</th><th>Student</th><th>Amount</th><th>Status</th></tr></thead><tbody>@forelse($recentPayments as $payment)<tr><td>{{ \Carbon\Carbon::parse($payment->created_at)->format('d M Y, H:i') }}</td><td>{{ $payment->student_name ?: 'Learner unavailable' }}</td><td><strong>KES {{ number_format((float)$payment->amount,2) }}</strong></td><td><span class="status-pill status-{{ strtolower($payment->status ?? 'unknown') }}">{{ ucfirst($payment->status ?? '—') }}</span></td></tr>@empty<tr><td colspan="4"><div class="empty-state">No recent payments found.</div></td></tr>@endforelse</tbody></table></div></section>
            <section class="card report-section compact-report"><div class="section-head"><div><span class="page-kicker">Latest applications</span><h2>Admissions activity</h2></div><a class="executive-link" href="{{ route('admin.reports',['report'=>'admissions']) }}">View all →</a></div><div class="table-wrap"><table class="table"><thead><tr><th>Date</th><th>Applicant</th><th>Class</th><th>Status</th></tr></thead><tbody>@forelse($recentApplications as $application)<tr><td>{{ \Carbon\Carbon::parse($application->created_at)->format('d M Y') }}</td><td><strong>{{ $application->student_name }}</strong></td><td>{{ $application->requested_class }}</td><td><span class="status-pill status-{{ strtolower($application->status ?? 'unknown') }}">{{ ucfirst($application->status ?? '—') }}</span></td></tr>@empty<tr><td colspan="4"><div class="empty-state">No recent applications found.</div></td></tr>@endforelse</tbody></table></div></section>
        </div>
    @endif

    @if($report === 'students')
        <section class="card report-section"><div class="section-head"><div><span class="page-kicker">Learner register</span><h2>Students</h2><p>Current learner register and fee balances.</p></div><span class="record-count">{{ number_format($students->total()) }} records</span></div><div class="table-wrap"><table class="table"><thead><tr><th>Admission</th><th>Name</th><th>Class</th><th>Parent / guardian</th><th>Balance</th></tr></thead><tbody>@forelse($students as $student)<tr><td>{{ $student->admission_number }}</td><td><strong>{{ $student->name }}</strong></td><td>{{ $student->class_label ? $student->class_label . ($student->stream ? ' - '.$student->stream : '') : ($student->class_name ?: '—') }}</td><td>{{ $student->parent_label ?: '—' }}</td><td>KES {{ number_format((float)($student->fee_balance ?? 0),2) }}</td></tr>@empty<tr><td colspan="5"><div class="empty-state">No students found.</div></td></tr>@endforelse</tbody></table></div>@include('admin.reports.partials.pagination',['paginator'=>$students,'label'=>'students'])</section>
    @endif

    @if($report === 'fees')
        <section class="card report-section"><div class="section-head"><div><span class="page-kicker">Financial reporting</span><h2>Fees & Payments</h2><p>Payment activity and M-Pesa references, with all records paginated.</p></div><span class="record-count">{{ number_format($payments->total()) }} payments</span></div><div class="report-finance-strip"><span>Successful/completed <strong>KES {{ number_format($paymentTotal,2) }}</strong></span><span>Outstanding <strong>KES {{ number_format($outstanding,2) }}</strong></span></div><div class="table-wrap"><table class="table"><thead><tr><th>Date</th><th>Student</th><th>Amount</th><th>Status</th><th>M-Pesa receipt</th></tr></thead><tbody>@forelse($payments as $payment)<tr><td>{{ $payment->created_at }}</td><td>{{ $payment->student_name ?: $payment->student_id }}</td><td><strong>KES {{ number_format((float)($payment->{$paymentAmountColumn} ?? 0),2) }}</strong></td><td><span class="status-pill status-{{ strtolower($payment->status ?? 'unknown') }}">{{ ucfirst($payment->status ?? '—') }}</span></td><td>{{ $payment->mpesa_receipt ?? '—' }}</td></tr>@empty<tr><td colspan="5"><div class="empty-state">No payments found.</div></td></tr>@endforelse</tbody></table></div>@include('admin.reports.partials.pagination',['paginator'=>$payments,'label'=>'payments'])</section>
    @endif

    @if($report === 'attendance')
        <section class="card report-section"><div class="section-head"><div><span class="page-kicker">Learner participation</span><h2>Attendance</h2><p>Daily attendance records with status breakdown.</p></div><span class="record-count">{{ number_format($attendance->total()) }} records</span></div><div class="status-grid report-statuses">@foreach(['present'=>'Present','absent'=>'Absent','late'=>'Late','excused'=>'Excused'] as $key=>$label)<div><span>{{ $label }}</span><strong>{{ number_format($attendanceSummary[$key] ?? 0) }}</strong></div>@endforeach</div><div class="table-wrap"><table class="table"><thead><tr><th>Date</th><th>Student</th><th>Status</th></tr></thead><tbody>@forelse($attendance as $row)<tr><td>{{ $row->attendance_date }}</td><td>{{ $row->student_name }}</td><td><span class="status-pill status-{{ strtolower($row->status) }}">{{ ucfirst($row->status) }}</span></td></tr>@empty<tr><td colspan="3"><div class="empty-state">No attendance records found.</div></td></tr>@endforelse</tbody></table></div>@include('admin.reports.partials.pagination',['paginator'=>$attendance,'label'=>'attendance records'])</section>
    @endif

    @if($report === 'results')
        <section class="card report-section cbc-report"><div class="section-head"><div><span class="page-kicker">Competency-Based Assessment</span><h2>CBC Academic Achievement Report</h2><p>Learning areas are reported using achievement levels, points and teacher/assessor evidence. A missed assessment remains distinct from a zero score.</p></div><span class="record-count">{{ number_format($results->total()) }} results</span></div><div class="result-highlight"><div><span>Average score</span><strong>{{ number_format($averageMarks,1) }}%</strong></div><div><span>Missed assessments</span><strong>{{ number_format($missedAssessmentCount) }}</strong></div></div><div class="cbc-summary">@foreach(['EE1'=>'Exceeding 1','EE2'=>'Exceeding 2','ME1'=>'Meeting 1','ME2'=>'Meeting 2','AE1'=>'Approaching 1','AE2'=>'Approaching 2','BE1'=>'Below 1','BE2'=>'Below 2'] as $code=>$label)<div><b>{{ $code }}</b><strong>{{ $cbcLevelSummary[$code]??0 }}</strong><small>{{ $label }}</small></div>@endforeach</div><div class="table-wrap"><table class="table"><thead><tr><th>Assessment</th><th>Learner</th><th>Learning area</th><th>Score</th><th>Achievement level</th><th>Points</th><th>Evidence / status</th></tr></thead><tbody>@forelse($results as $row)@php $missed=$row->assessment_status==='missed'; $code=$missed?'MISSED':($row->achievement_level?:'—'); $label=$missed?'Missed Assessment':$code; @endphp<tr><td>{{ $row->exam_name ?: 'Assessment unavailable' }}</td><td><strong>{{ $row->student_name ?: 'Learner unavailable' }}</strong><small>{{ $row->admission_number ?: 'No admission number' }}</small></td><td>{{ $row->subject_name ?: 'Learning area unavailable' }}</td><td><strong>{{ $row->marks===null?'—':number_format((float)$row->marks,1).'/100' }}</strong></td><td><span class="achievement achievement-{{ strtolower($code) }}">{{ $code }}</span><small>{{ $label }}</small></td><td>{{ $row->achievement_points===null?'—':$row->achievement_points }}</td><td>{{ $missed?'Missed assessment — follow-up required.':'Achievement level calculated from score.' }}</td></tr>@empty<tr><td colspan="7"><div class="empty-state">No CBC results found.</div></td></tr>@endforelse</tbody></table></div>@include('admin.reports.partials.pagination',['paginator'=>$results,'label'=>'CBC results'])</section>
    @endif

    @if($report === 'admissions')
        <section class="card report-section"><div class="section-head"><div><span class="page-kicker">Enrolment pipeline</span><h2>Admissions</h2><p>Application workflow and requested classes.</p></div><span class="record-count">{{ number_format($applications->total()) }} applications</span></div><div class="status-grid report-statuses admissions-status">@foreach(['approved'=>'Approved','pending'=>'Pending','rejected'=>'Rejected'] as $key=>$label)<div><span>{{ $label }}</span><strong>{{ number_format($applicationSummary[$key] ?? 0) }}</strong></div>@endforeach</div><div class="table-wrap"><table class="table"><thead><tr><th>Date</th><th>Student</th><th>Requested class</th><th>Status</th></tr></thead><tbody>@forelse($applications as $application)<tr><td>{{ $application->created_at }}</td><td><strong>{{ $application->student_name }}</strong></td><td>{{ $application->requested_class }}</td><td><span class="status-pill status-{{ strtolower($application->status) }}">{{ ucfirst($application->status) }}</span></td></tr>@empty<tr><td colspan="4"><div class="empty-state">No applications found.</div></td></tr>@endforelse</tbody></table></div>@include('admin.reports.partials.pagination',['paginator'=>$applications,'label'=>'admission applications'])</section>
    @endif
</div>
<style>
.reports-shell{--report-ink:#12233a;--report-muted:#65758b;--report-line:#e2e9f2;--report-surface:#fff;--report-soft:#f5f8fc;--report-brand:#1769df;--report-brand-dark:#0d4fae;--report-success:#137a52;--report-warning:#a76500;--report-danger:#b42318;color:var(--report-ink)}
.report-hero{margin-bottom:18px}.report-kicker,.page-kicker{display:inline-block;color:var(--report-brand);font-size:10px;font-weight:900;letter-spacing:.12em;text-transform:uppercase}.report-hero h1{margin:5px 0 6px;letter-spacing:-.035em}.report-hero p{margin:0;color:var(--report-muted);max-width:820px;line-height:1.55}.report-tabs{display:flex;gap:6px;overflow:auto;padding:5px;margin:0 0 18px;background:var(--report-soft);border:1px solid var(--report-line);border-radius:14px}.report-tabs a{white-space:nowrap;padding:10px 13px;border-radius:10px;color:var(--report-muted);font-weight:800;text-decoration:none}.report-tabs a:hover{background:#fff;color:var(--report-brand)}.report-tabs a.active{background:#fff;color:var(--report-brand);box-shadow:0 5px 16px rgba(18,39,70,.08)}.report-stats{margin-bottom:18px}.report-stat{position:relative;min-height:112px;border:1px solid var(--report-line);box-shadow:0 10px 28px rgba(18,39,70,.06);overflow:hidden}.report-stat .stat-icon{position:absolute;right:16px;top:14px;width:34px;height:34px;display:grid;place-items:center;border-radius:10px;background:#edf4ff;color:var(--report-brand);font-size:11px;font-weight:900}.report-stat strong{display:block;margin:8px 0 3px;font-size:25px}.report-stat small{color:var(--report-muted);font-size:11px}.executive-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px;margin-bottom:18px}.executive-card{border:1px solid var(--report-line);box-shadow:0 10px 28px rgba(18,39,70,.05);min-height:220px}.section-head{display:flex;justify-content:space-between;align-items:flex-start;gap:16px}.section-head h2{margin:3px 0 5px;letter-spacing:-.025em}.section-head p{margin:0;color:var(--report-muted);font-size:12px;line-height:1.55}.insight-icon{display:grid;place-items:center;min-width:40px;height:40px;border-radius:12px;background:#edf4ff;color:var(--report-brand);font-size:10px;font-weight:900}.finance-values{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin:22px 0}.finance-values>div,.mini-metrics>div,.status-grid>div{padding:14px;border:1px solid var(--report-line);border-radius:12px;background:var(--report-soft)}.finance-values span,.mini-metrics span,.status-grid span{display:block;color:var(--report-muted);font-size:10px;font-weight:750}.finance-values strong{display:block;margin-top:5px;font-size:19px}.mini-metrics{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin:22px 0}.mini-metrics strong{display:block;font-size:22px;margin-bottom:4px}.status-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:9px;margin:22px 0}.status-grid strong{display:block;margin-top:5px;font-size:18px}.executive-link{color:var(--report-brand);font-size:11px;font-weight:850;text-decoration:none}.executive-link:hover{text-decoration:underline}.executive-achievement{margin-bottom:18px}.executive-lower{display:grid;grid-template-columns:1fr 1fr;gap:18px}.compact-report{margin-bottom:0}.record-count{padding:7px 10px;border-radius:999px;background:#edf4ff;color:var(--report-brand);font-size:10px;font-weight:900;white-space:nowrap}.report-section{margin-bottom:18px;border:1px solid var(--report-line);box-shadow:0 10px 28px rgba(18,39,70,.05)}.report-section .table-wrap{overflow-x:auto;margin-top:16px}.report-section table{min-width:720px}.report-section th{background:#f7f9fc;color:#53647b;font-size:10px;text-transform:uppercase;letter-spacing:.05em}.report-section td{vertical-align:middle}.report-section td small{display:block;color:#8491a3;font-size:10px;margin-top:3px}.status-pill,.achievement{display:inline-flex;align-items:center;justify-content:center;padding:5px 8px;border-radius:999px;font-size:10px;font-weight:900;white-space:nowrap}.status-pill{background:#eef2f7;color:#53647b}.status-present,.status-paid,.status-completed,.status-published,.status-approved{background:#e8f7ef;color:var(--report-success)}.status-absent,.status-failed,.status-rejected,.status-cancelled{background:#fdeceb;color:var(--report-danger)}.status-late,.status-pending,.status-draft{background:#fff4dc;color:var(--report-warning)}.cbc-summary{display:grid;grid-template-columns:repeat(8,1fr);gap:9px;margin:18px 0 0}.cbc-summary>div{padding:12px 8px;background:var(--report-soft);border:1px solid var(--report-line);border-radius:11px;text-align:center}.cbc-summary b{display:block;color:var(--report-brand);font-size:11px}.cbc-summary strong{display:block;font-size:18px;margin:3px 0}.cbc-summary small{display:block;color:var(--report-muted);font-size:9px}.achievement{background:#edf4ff;color:var(--report-brand)}.achievement-missed{background:#fff4dc;color:var(--report-warning)}.report-finance-strip,.result-highlight{display:flex;gap:10px;flex-wrap:wrap;margin-top:16px}.report-finance-strip span,.result-highlight>div{padding:10px 13px;border-radius:10px;background:#f5f8fc;border:1px solid var(--report-line);font-size:10px;color:var(--report-muted)}.report-finance-strip strong,.result-highlight strong{margin-left:5px;color:var(--report-ink);font-size:12px}.report-statuses{margin-bottom:0}.empty-state{border:1px dashed #ccd7e5;border-radius:13px;padding:24px;text-align:center;color:#7c899c;background:#fafbfd;font-size:12px}.admin-main .stat strong{font-size:24px}
@media(max-width:1000px){.cbc-summary{grid-template-columns:repeat(4,1fr)}}@media(max-width:800px){.executive-grid,.executive-lower{grid-template-columns:1fr}.executive-card{min-height:0}}@media(max-width:600px){.report-tabs{border-radius:12px}.cbc-summary{grid-template-columns:repeat(2,1fr)}.report-hero .admin-actions{width:100%}.report-hero .admin-actions .btn{flex:1;text-align:center}.report-section,.executive-card{padding:15px}.report-section table{min-width:650px}.status-grid{grid-template-columns:repeat(2,1fr)}.finance-values,.mini-metrics{grid-template-columns:1fr}}
@media print{.sidebar,.admin-top,.admin-breadcrumb,.admin-actions,.report-tabs,.report-pagination,.pagination-controls{display:none!important}.admin-main{padding:0}.report-section,.report-stat,.executive-card{box-shadow:none!important;break-inside:avoid}.report-section table{min-width:0}}
</style>
@endsection
