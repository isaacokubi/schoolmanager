@extends('layouts.app')
@section('title','Admissions | School Manager')
@section('body')
<div class="admin-shell"><header class="admin-top"><strong>Admissions</strong><a style="color:#fff" href="{{ route('admin.dashboard') }}">Dashboard</a></header><main class="admin-main"><div class="card"><form method="get" class="grid"><input name="search" value="{{ request('search') }}" placeholder="Search applicant or parent"><select name="status"><option value="">All statuses</option>@foreach(['pending','approved','rejected'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="btn" type="submit">Filter</button></form></div><br>@if(session('success'))<div class="card">{{ session('success') }}</div><br>@endif<div class="card"><h2>Applications</h2><div class="table-wrap"><table class="table"><thead><tr><th>Student</th><th>DOB</th><th>Parent</th><th>Phone</th><th>Class</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($applications as $a)<tr><td>{{ $a->student_name }}</td><td>{{ $a->date_of_birth ?: '—' }}</td><td>{{ $a->parent_name }}</td><td>{{ $a->parent_phone }}</td><td>{{ $a->requested_class ?: '—' }}</td><td>{{ ucfirst($a->status) }}</td><td><form method="post" action="{{ route('admin.admissions.status',$a->id) }}">@csrf @method('PATCH')<select name="status"><option value="pending">Pending</option><option value="approved">Approve</option><option value="rejected">Reject</option></select><button type="submit">Save</button></form></td></tr>@empty<tr><td colspan="7">No applications found.</td></tr>@endforelse</tbody></table></div>{{ $applications->links() }}</div></main></div>
@endsection
