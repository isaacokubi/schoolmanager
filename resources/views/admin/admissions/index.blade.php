@extends('layouts.admin')
@section('title','Admissions | School Manager')
@section('page_title','Admissions')
@section('admin_content')
<div class="admin-page-head"><div><h1>Admissions</h1><p>Review, approve and manage new student applications.</p></div><a class="btn secondary" href="{{ route('admin.dashboard') }}">← Dashboard</a></div>
@if(session('success'))<div class="alert success">{{ session('success') }}</div>@endif @if($errors->any())<div class="alert error">{{ $errors->first() }}</div>@endif
<div class="card" style="margin-bottom:16px"><form method="get" class="search"><input name="search" value="{{ request('search') }}" placeholder="Search applicant or parent"><select name="status"><option value="">All statuses</option>@foreach(['pending','approved','rejected'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select><button type="submit">Filter</button>@if(request('search')||request('status'))<a href="{{ route('admin.admissions.index') }}">Clear</a>@endif</form></div>
<div class="card"><div class="section-head"><div><h2>Applications</h2><p>Process applications and keep admission decisions up to date.</p></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Applicant</th><th>Parent / Phone</th><th>Class</th><th>Status</th><th>Decision</th></tr></thead><tbody>@forelse($applications as $a)<tr><td><strong>{{ $a->student_name }}</strong><br><small class="muted">DOB: {{ $a->date_of_birth ?: '—' }}</small></td><td>{{ $a->parent_name }}<br><small class="muted">{{ $a->parent_phone }}</small></td><td>{{ $a->requested_class ?: '—' }}</td><td><span class="status-badge {{ $a->status==='approved'?'success':($a->status==='rejected'?'danger':'warning') }}">{{ ucfirst($a->status) }}</span></td><td><form method="post" action="{{ route('admin.admissions.status',$a->id) }}" style="display:flex;gap:7px;align-items:center">@csrf @method('PATCH')<select name="status"><option value="pending" @selected($a->status==='pending')>Pending</option><option value="approved" @selected($a->status==='approved')>Approve</option><option value="rejected" @selected($a->status==='rejected')>Reject</option></select><button class="btn" type="submit">Save</button></form></td></tr>@empty<tr><td colspan="5"><div class="empty-state"><strong>No applications found</strong><span>New applications submitted on the public site will appear here.</span></div></td></tr>@endforelse</tbody></table></div>{{ $applications->links() }}</div>
@endsection