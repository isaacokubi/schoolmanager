@extends('layouts.app')
@section('title','Payments | School Manager')
@section('body')
<div class="admin-shell"><header class="admin-top"><strong>Fees & Payments</strong><a style="color:#fff" href="{{ route('admin.dashboard') }}">Dashboard</a></header><main class="admin-main"><p><a class="btn" href="{{ route('admin.payments.create') }}">+ Record Payment</a></p>@if(session('success'))<div class="card">{{ session('success') }}</div><br>@endif<div class="card"><form method="get" class="grid"><input name="search" value="{{ request('search') }}" placeholder="Search student, admission no., phone or receipt"><select name="status"><option value="">All statuses</option>@foreach(['pending','completed','failed'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="btn" type="submit">Filter</button></form></div><br><div class="card"><h2>Payments</h2><div class="table-wrap"><table class="table"><thead><tr><th>Date</th><th>Student</th><th>Admission</th><th>Type</th><th>Amount</th><th>Reference</th><th>Status</th></tr></thead><tbody>@forelse($payments as $p)<tr><td>{{ $p->created_at }}</td><td>{{ $p->student_name ?: '—' }}</td><td>{{ $p->admission_number ?: '—' }}</td><td>{{ $p->payment_type }}</td><td>KES {{ number_format((float)$p->amount,2) }}</td><td>{{ $p->mpesa_receipt ?: ($p->account_reference ?: '—') }}</td><td>{{ ucfirst($p->status) }}</td></tr>@empty<tr><td colspan="7">No payments found.</td></tr>@endforelse</tbody></table></div>{{ $payments->links() }}</div></main></div>
@endsection
