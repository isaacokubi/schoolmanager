@extends('layouts.app')
@section('title', $student ? 'Edit Student' : 'Add Student')
@section('body')
<div class="admin-shell"><header class="admin-top"><strong>{{ $student ? 'Edit Student' : 'Add Student' }}</strong><a style="color:#fff" href="{{ route('admin.students.index') }}">Students</a></header><main class="admin-main"><div class="card"><form method="post" action="{{ $student ? route('admin.students.update',$student->id) : route('admin.students.store') }}">@csrf @if($student)<input type="hidden" name="_method" value="PUT">@endif<div class="grid"><label>Admission Number<input name="admission_number" required value="{{ old('admission_number',$student->admission_number ?? '') }}">@error('admission_number')<small>{{ $message }}</small>@enderror</label><label>Student Name<input name="name" required value="{{ old('name',$student->name ?? '') }}">@error('name')<small>{{ $message }}</small>@enderror</label><label>Class<input name="class_name" value="{{ old('class_name',$student->class_name ?? '') }}"></label><label>Parent/Guardian<input name="parent_name" value="{{ old('parent_name',$student->parent_name ?? '') }}"></label><label>Parent Phone<input name="parent_phone" placeholder="+2547XXXXXXXX" value="{{ old('parent_phone',$student->parent_phone ?? '') }}">@error('parent_phone')<small>{{ $message }}</small>@enderror</label><label>Fee Balance<input type="number" min="0" step="0.01" name="fee_balance" value="{{ old('fee_balance',$student->fee_balance ?? 0) }}"></label></div><br><button class="btn" type="submit">{{ $student ? 'Update Student' : 'Save Student' }}</button></form></div></main></div>
@endsection
