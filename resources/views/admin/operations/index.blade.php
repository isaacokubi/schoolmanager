@extends('layouts.admin')
@section('title','School Operations | '.config('app.name'))
@section('page_title','Operations')
@section('admin_content')
<div class="admin-page-head"><div><span class="page-kicker">School operations</span><h1>Academic & School Records</h1><p>Manage CBC assessments, attendance, learners, staff and school communication.</p></div><a class="btn secondary" href="{{ route('admin.dashboard') }}">← Dashboard</a></div>
@if(session('success'))<div class="alert success">{{ session('success') }}</div>@endif
@if($errors->any())<div class="alert error">{{ $errors->first() }}</div>@endif
<div class="ops-nav">@foreach(['parents'=>'Parents','classes'=>'Classes','teachers'=>'Teachers','subjects'=>'Learning Areas','attendance'=>'Attendance','exams'=>'Assessments','results'=>'CBC Results','announcements'=>'Announcements','events'=>'Events'] as $key=>$label)<a href="{{ route('admin.operations',['section'=>$key]) }}" class="{{ $section===$key?'active':'' }}">{{ $label }}</a>@endforeach</div>
@php$configs=['parents'=>['title'=>'Parents / Guardians','fields'=>[['name','Name','text',true],['phone','Phone','text',true],['email','Email','email',false],['relationship','Relationship','text',false]]],'classes'=>['title'=>'Classes','fields'=>[['name','Class name','text',true],['stream','Stream','text',false],['academic_year','Academic year','number',false]]],'teachers'=>['title'=>'Teachers','fields'=>[['name','Name','text',true],['email','Email','email',false],['phone','Phone','text',false],['employee_number','Employee number','text',false]]],'subjects'=>['title'=>'Learning Areas','fields'=>[['name','Learning area / subject','text',true],['code','Code','text',false]]],'exams'=>['title'=>'Assessment','fields'=>[['name','Assessment name','text',true],['term','Term','text',true],['academic_year','Academic year','number',true],['start_date','Start date','date',false],['end_date','End date','date',false]]],'announcements'=>['title'=>'Announcements','fields'=>[['title','Title','text',true],['body','Message','textarea',true]]],'events'=>['title'=>'Events','fields'=>[['title','Title','text',true],['event_date','Date','date',true],['location','Location','text',false],['description','Description','textarea',false]]]];$records=${$section};@endphp
@if(isset($configs[$section]))@php($cfg=$configs[$section])<div class="card form-card"><div class="section-head"><div><span class="page-kicker">Create</span><h2>{{ $cfg['title'] }}</h2></div></div><form method="POST" action="{{ route('admin.operations.store') }}" class="grid-form">@csrf<input type="hidden" name="section" value="{{ $section }}">@foreach($cfg['fields'] as $f)<label>{{ $f[1] }}@if($f[2]==='textarea')<textarea name="{{ $f[0] }}" {{ $f[3]?'required':'' }}>{{ old($f[0]) }}</textarea>@else<input type="{{ $f[2] }}" name="{{ $f[0] }}" value="{{ old($f[0]) }}" {{ $f[3]?'required':'' }}>@endif</label>@endforeach @if($section==='subjects')<label>Teacher<select name="teacher_id"><option value="">Unassigned</option>@foreach($teachers as $teacher)<option value="{{ $teacher->id }}">{{ $teacher->name }}</option>@endforeach</select></label>@endif @if($section==='announcements')<label class="check"><input type="checkbox" name="published" value="1" checked> Publish now</label>@endif<button type="submit">Add Record</button></form></div>@endif
@if($section==='attendance')<div class="card form-card"><div class="section-head"><div><span class="page-kicker">Daily records</span><h2>Record Attendance</h2><p>Capture present, absent, late or excused attendance.</p></div></div><form method="POST" action="{{ route('admin.operations.attendance') }}" class="grid-form">@csrf<label>Student<select name="student_id" required><option value="">Select student</option>@foreach($students as $student)<option value="{{ $student->id }}">{{ $student->name }} — {{ $student->admission_number }}</option>@endforeach</select></label><label>Date<input type="date" name="attendance_date" value="{{ date('Y-m-d') }}" required></label><label>Status<select name="status" required><option value="present">Present</option><option value="absent">Absent</option><option value="late">Late</option><option value="excused">Excused</option></select></label><label>Notes<textarea name="notes"></textarea></label><button type="submit">Save Attendance</button></form></div>@endif
@if($section==='results')<div class="card form-card cbc-entry"><div class="section-head"><div><span class="page-kicker">Competency-Based Assessment</span><h2>Record CBC Assessment</h2><p>Enter a score or explicitly mark the learner as <strong>Missed</strong>. The system calculates the CBC achievement level automatically.</p></div><div class="cbc-legend"><b>EE</b> Exceeding · <b>ME</b> Meeting · <b>AE</b> Approaching · <b>BE</b> Below</div></div><form method="POST" action="{{ route('admin.operations.results') }}" class="grid-form" id="cbc-form">@csrf<label>Assessment<select name="exam_id" required><option value="">Select assessment</option>@foreach($exams as $exam)<option value="{{ $exam->id }}">{{ $exam->name }} — {{ $exam->term }} {{ $exam->academic_year }}</option>@endforeach</select></label><label>Learner<select name="student_id" required><option value="">Select learner</option>@foreach($students as $student)<option value="{{ $student->id }}">{{ $student->name }} — {{ $student->admission_number }}</option>@endforeach</select></label><label>Learning area<select name="subject_id" required><option value="">Select learning area</option>@foreach($subjects as $subject)<option value="{{ $subject->id }}">{{ $subject->name }}</option>@endforeach</select></label><label>Assessment status<select name="assessment_status" id="assessment-status" required><option value="present">Assessment completed</option><option value="missed">Missed assessment</option></select></label><label id="marks-field">Score (0–100)<input type="number" name="marks" id="marks" min="0" max="100" step="0.01"></label><label>Teacher / assessor remarks<textarea name="remarks" placeholder="Optional learning feedback"></textarea></label><button type="submit">Save CBC Assessment</button></form><div class="cbc-scale"><div><b>EE1</b><span>90–100</span></div><div><b>EE2</b><span>75–89</span></div><div><b>ME1</b><span>58–74</span></div><div><b>ME2</b><span>41–57</span></div><div><b>AE1</b><span>31–40</span></div><div><b>AE2</b><span>21–30</span></div><div><b>BE1</b><span>11–20</span></div><div><b>BE2</b><span>0–10</span></div></div></div><script>document.getElementById('assessment-status').addEventListener('change',function(){let m=document.getElementById('marks');m.required=this.value==='present';m.disabled=this.value==='missed';if(this.value==='missed')m.value='';});</script>@endif
<div class="card"><div class="section-head"><div><span class="page-kicker">Records</span><h2>{{ $section==='results'?'CBC assessment results':ucfirst($section) }}</h2><p>Search and review existing records.</p></div></div><form method="GET" class="search"><input type="hidden" name="section" value="{{ $section }}"><input name="search" value="{{ request('search') }}" placeholder="Search..."><button type="submit">Search</button>@if(request('search'))<a href="{{ route('admin.operations',['section'=>$section]) }}">Clear</a>@endif</form><div class="table-wrap" style="margin-top:16px"><table class="table"><thead><tr><th>ID</th><th>Record</th><th>Actions</th></tr></thead><tbody>@forelse($records as $row)<tr><td>{{ $row->id }}</td><td>@if($section==='attendance')<strong>{{ $row->student_name }}</strong><br><small>{{ $row->attendance_date }} · {{ ucfirst($row->status) }}</small>@elseif($section==='results')<strong>{{ $row->student_name }}</strong><br><small>{{ $row->subject_name }} · {{ $row->exam_name }}</small><div class="cbc-result"><b>{{ $row->assessment_status==='missed'?'MISSED':($row->achievement_level ?: '—') }}</b>@if($row->marks!==null)<span>{{ number_format((float)$row->marks,2) }}/100</span>@endif@if($row->achievement_points)<span>{{ $row->achievement_points }} pts</span>@endif</div>@else @foreach((array)$row as $k=>$v) @if(!in_array($k,['id','created_at','updated_at'],true))<strong>{{ ucwords(str_replace('_',' ',$k)) }}:</strong> {{ \Illuminate\Support\Str::limit((string)$v,100) }} · @endif @endforeach @endif</td><td>@if($section==='results')<a class="btn ghost small" href="{{ route('admin.report-cards.show',[$row->student_id,$row->exam_id]) }}">View report</a><a class="btn secondary small" href="{{ route('admin.report-cards.download',[$row->student_id,$row->exam_id]) }}">Download</a><form method="POST" action="{{ route('admin.report-cards.notify',[$row->student_id,$row->exam_id]) }}" style="display:inline">@csrf<button class="btn secondary small" type="submit">Send</button></form>@endif</td></tr>@empty<tr><td colspan="3"><div class="empty-state">No records found.</div></td></tr>@endforelse</tbody></table></div>{{ $records->links() }}</div>
<style>.cbc-entry{border:1px solid #cfe0f7;background:linear-gradient(180deg,#fff,#f8fbff)}.cbc-legend{background:#edf5ff;padding:9px 12px;border-radius:10px;font-size:11px;color:#52637a}.cbc-scale{display:grid;grid-template-columns:repeat(8,1fr);gap:7px;margin-top:17px}.cbc-scale div{background:#fff;border:1px solid #e2e9f2;border-radius:9px;text-align:center;padding:8px}.cbc-scale b{display:block;color:#1769df;font-size:11px}.cbc-scale span{font-size:9px;color:#7d8a9d}.cbc-result{display:flex;gap:7px;align-items:center;margin-top:6px}.cbc-result b{background:#edf5ff;color:#1769df;border-radius:7px;padding:4px 7px;font-size:10px}.cbc-result span{font-size:10px;color:#738198}.small{padding:7px 10px;font-size:10px;margin-right:4px}@media(max-width:900px){.cbc-scale{grid-template-columns:repeat(4,1fr)}}@media(max-width:600px){.cbc-scale{grid-template-columns:repeat(2,1fr)}.cbc-legend{margin-top:10px}}</style>
@endsection
