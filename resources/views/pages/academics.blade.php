@extends('layouts.public')
@section('title','Academics | '.$settings['school_name'])
@section('content')
<section class="page-hero"><div class="container"><p class="eyebrow">Academic programme</p><h1>Learning at {{ $settings['school_name'] }}</h1><p>{{ $settings['academic_year'] ? 'Academic year '.$settings['academic_year'].' · '.$settings['academic_term'] : 'Explore our current classes and learning areas.' }}</p></div></section>
<section class="section"><div class="container"><div class="section-head"><div><p class="eyebrow">Class structure</p><h2>Classes & streams</h2><p>Current class information is sourced directly from the school management system.</p></div></div><div class="grid">@forelse($classes as $class)<article class="card"><span class="badge">{{ $class->academic_year ?: 'Current' }}</span><h3 style="margin-top:14px">{{ $class->name }}</h3><p class="muted">{{ $class->stream ?: 'General stream' }}</p></article>@empty<div class="empty"><h3>No classes published yet</h3><p>The school administration has not added classes to the public academic programme.</p></div>@endforelse</div></div></section>
<section class="section alt"><div class="container"><div class="section-head"><div><p class="eyebrow">Curriculum</p><h2>Subjects & learning areas</h2><p>Subjects and their assigned teachers are maintained in the administration system.</p></div></div><div class="grid">@forelse($subjects as $subject)<article class="card"><div class="icon">{{ strtoupper(substr($subject->name,0,1)) }}</div><h3>{{ $subject->name }}</h3><p class="muted">{{ $subject->code ? 'Code: '.$subject->code : 'Learning area' }}</p>@if($subject->teacher_name)<small>Teacher: <strong>{{ $subject->teacher_name }}</strong></small>@endif</article>@empty<div class="empty"><h3>No subjects published yet</h3><p>Learning areas will appear here when they are configured by the school.</p></div>@endforelse</div></div></section>
<section class="section"><div class="container"><div class="card"><div class="split"><div><p class="eyebrow">Admissions</p><h2>Ready to join {{ $settings['school_name'] }}?</h2><p class="muted">Submit an application online and the admissions team will review your details.</p></div><div style="display:flex;align-items:center;justify-content:flex-end"><a class="btn" href="{{ route('admissions') }}">Apply for admission →</a></div></div></div></div></section>
@endsection